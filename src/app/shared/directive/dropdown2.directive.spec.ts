import { Dropdown2Directive } from './dropdown2.directive';

describe('Dropdown2Directive', () => {
  it('should create an instance', () => {
    const directive = new Dropdown2Directive();
    expect(directive).toBeTruthy();
  });
});
