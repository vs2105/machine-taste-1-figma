import { ToggletableandformDirective } from './toggletableandform.directive';

describe('ToggletableandformDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggletableandformDirective();
    expect(directive).toBeTruthy();
  });
});
