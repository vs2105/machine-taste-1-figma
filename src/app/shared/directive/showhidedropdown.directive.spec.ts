import { ShowhidedropdownDirective } from './showhidedropdown.directive';

describe('ShowhidedropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowhidedropdownDirective();
    expect(directive).toBeTruthy();
  });
});
