import { ShelftoogrleDirective } from './shelftoogrle.directive';

describe('ShelftoogrleDirective', () => {
  it('should create an instance', () => {
    const directive = new ShelftoogrleDirective();
    expect(directive).toBeTruthy();
  });
});
